---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: scripts.sre.henrywhitaker.com
spec:
  group: sre.henrywhitaker.com
  names:
    kind: Script
    listKind: ScriptList
    plural: scripts
    singular: script
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Script is the Schema for the scripts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScriptSpec defines the desired state of Script
            properties:
              image:
                type: string
              script:
                description: The script to run when triggered
                type: string
              secrets:
                items:
                  type: string
                type: array
              shell:
                type: string
              throttle:
                description: Throttle the action to 1 time per specified duration
                  e.g. 5m, 1h
                type: string
              triggers:
                description: A list of triggers that this script subscribes to
                items:
                  properties:
                    kind:
                      type: string
                    name:
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
            required:
            - script
            - triggers
            type: object
          status:
            description: ScriptStatus defines the observed state of Script
            properties:
              error:
                type: string
              registered:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
